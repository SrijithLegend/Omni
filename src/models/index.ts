/**
 * Omni Data Models — All domain objects and their schemas.
 *
 * These are the canonical shapes of data that flow through the entire
 * extension. Every engine, every UI component, every storage adapter
 * must use these types.
 */

export * from "./universal-conversation";
